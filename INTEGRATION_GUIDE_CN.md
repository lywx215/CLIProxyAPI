# 前后端联动开发指南

本文档为 `CLIProxyAPI` 后端与 `Cli-Proxy-API-Management-Center` 前端的建议性联动开发清单。

> 适用环境：以单人开发为主。
> 本文不是审批流程，也不应仅因配套仓库、完整测试环境或联调条件暂时不可用而阻止当前修改继续进行。

## 1. 文档边界

- 日常功能开发、接口调整和管理页面装配参考本文。
- 从原项目同步 fork 时，遵循 [Fork Synchronization Guide](SYNC_GUIDE.md)。同步指南中的分支保护、冲突处理和发布授权规则不因本文而放宽。
- 两个仓库继续独立提交、独立维护 Git 历史；不要求为了联动开发改造成 monorepo 或 submodule。

## 2. 规则强度

本文使用以下三级表述：

- **必须**：仅用于安全、数据正确性和正式发布条件。
- **建议**：默认做法，可以根据修改范围和当前环境调整。
- **可选**：用于提高可追溯性，不完成也不阻断开发。

开发过程中允许先完成一个仓库的修改。如果另一个仓库暂时不可用，可以记录待验证事项并继续；准备发布受影响功能之前，再完成必要联调。

## 3. 判断是否需要联动

以下修改通常建议检查配套仓库：

- `/v0/management` 路由、请求参数、响应字段或错误语义。
- `config.yaml` 字段、默认值、校验、热更新或序列化行为。
- OAuth、凭证文件、provider key、模型映射和配额数据结构。
- 前端新增页面、菜单或操作依赖新的后端能力。
- `dist/index.html` 到后端 `static/management.html` 的装配和发布。

纯后端执行逻辑、纯前端样式、文案或不影响契约的重构通常可以只修改一个仓库，不要求创建配套改动。

## 4. 建议的开发方式

### 4.1 开始修改

建议先用几句话记录受影响的接口、配置或页面。任务编号、issue 和 PR 均为可选；如果使用编号，可以让两个仓库采用相同编号以便检索。

开发顺序不作强制要求：

- 接口已经明确时，可以先实现后端。
- 需要探索交互时，可以先做前端原型或 mock。
- 修改只涉及一个仓库时，可以直接完成该仓库的实现和测试。

### 4.2 接口兼容

建议优先使用向后兼容的增量变更：

- 新响应字段允许旧前端忽略。
- 新请求字段有合理默认值，旧请求仍可工作。
- 前端遇到不存在的可选端点时，显示不支持或隐藏对应功能，而不是破坏其他页面。
- 删除或重命名已使用字段前，先确认配套前端已经迁移。
- 可视化配置编辑应尽量保留未知 YAML 字段，避免写回时丢失后端新增配置。

如果同步升级必须引入破坏性变更，可以让前后端同时更新，不要求为了维持旧版本兼容而无限增加适配代码；发布说明中记录配套版本即可。

### 4.3 联调

跨仓功能建议在同时附加两个目录的 `CLIProxyAPI_ALL` 项目中联调。每个仓库仍分别检查状态和提交，避免把一个仓库的提交误认为已经包含另一个仓库的修改。

联调时按本次修改范围选择检查项：

- 前端请求路径、方法、参数与后端路由一致。
- 前端类型和解析逻辑能够处理真实后端响应。
- 配置读取、修改、保存和热更新保持一致。
- 后端返回不支持或缺少可选字段时，前端能够安全降级。
- 受影响页面在实际后端上完成一次主要流程。

## 5. 分级验证

### 5.1 日常开发

运行与修改直接相关的格式化、类型检查、单元测试或定向测试即可。不要仅因为无法运行另一个仓库的完整测试而停止当前实现；应说明尚未验证的部分。

### 5.2 准备联调

建议至少执行：

- 后端相关 Go 测试和编译。
- 前端 `bun run type-check`、相关测试和生产构建。
- 一次受影响 Management API 与页面的联合验证。

### 5.3 准备发布

正式发布受影响功能前必须确认：

- 两个仓库没有意外提交凭证、配置密钥或其他敏感文件。
- 计划发布的后端和前端组合能够完成受影响的主要流程。
- 前端单文件产物已从确定的前端提交构建，并与后端中的 `static/management.html` 一致。
- 无法执行的发布检查已经记录，且已判断不会造成已知的数据损坏或安全问题。

完整的旧版/新版兼容矩阵是可选项；只有本次修改确实需要兼容多个已部署版本时才执行。

## 6. 管理页面装配

不要假设两个仓库具有固定盘符或固定的相邻目录名称。通过 Codex 附加目录、Git remote 或开发者提供的位置确认仓库，再把实际路径赋给变量。

PowerShell 示例：

```powershell
$FrontendRepoPath = '<frontend-repository-path>'
$BackendRepoPath = '<backend-repository-path>'
$FrontendAssetPath = Join-Path $FrontendRepoPath 'dist/index.html'
$BackendAssetPath = Join-Path $BackendRepoPath 'static/management.html'

Copy-Item -LiteralPath $FrontendAssetPath -Destination $BackendAssetPath -Force
Get-FileHash -Algorithm SHA256 $FrontendAssetPath
Get-FileHash -Algorithm SHA256 $BackendAssetPath
```

Bash 示例：

```bash
FRONTEND_REPO_PATH='<frontend-repository-path>'
BACKEND_REPO_PATH='<backend-repository-path>'

cp "$FRONTEND_REPO_PATH/dist/index.html" "$BACKEND_REPO_PATH/static/management.html"
sha256sum "$FRONTEND_REPO_PATH/dist/index.html"
sha256sum "$BACKEND_REPO_PATH/static/management.html"
```

两个哈希应一致。构建时间可能改变产物内容，因此应在最后一次成功构建后再复制。记录前端 commit、tag 或哈希属于推荐做法，但日常未发布开发不强制维护额外 manifest。

## 7. 发布与回滚建议

建议的发布顺序是：

1. 确认后端接口和配置行为。
2. 确认配套前端提交并生成单文件产物。
3. 把产物装配到后端并完成联合验证。
4. 分别提交两个仓库，按实际部署方式发布。

开发者可以根据部署场景调整顺序。回滚时应同时确认后端代码、配置格式和管理页面仍然兼容；不要求两个仓库的版本号保持相同。

## 8. 简短检查清单

- [ ] 本次修改是否真的影响另一个仓库？
- [ ] 接口、配置和前端类型是否一致？
- [ ] 已运行与修改范围相称的测试？
- [ ] 未完成的联调或发布检查是否已记录？
- [ ] 发布时的 `management.html` 是否来自预期前端提交？
- [ ] 两个仓库是否分别检查并提交？

除第 5.3 节标明的发布条件外，本清单用于提醒，不作为继续开发的阻断门禁。
